# v0.60.1 post-Contents permission recheck

This is a NEW read-only observation, NOT a reset/retry of either consumed V1 attempt and NOT a publisher.

## Reason for successor

The operator reported that the PAT lacked Contents and that they added it. This is operator-reported metadata, not an independently verified GitHub permission or proof of the historical push failure's cause. The exact denied discovery (5004 bytes, SHA-256 d755106034cfc470a10d6d859c138f308bc0334f77453f159e3a61a9cc014158) and its consumed attempt (1107 bytes, SHA-256 7eed29fac84985b6d846759250027fd3f8cb30e055f75ced3931d97a023c79ea) are required inputs, in addition to the fixed original publication attempt/outcome and original preflight. Their status fields remain unchanged.

## Required new action

Zero-argument EXE outside the accepted repository. Existing exact 365-file MinGit only. Before new confirmation: local proof and original incident checks only. Confirm `RECHECK-CONTENTS-WRITE-V0601-NO-PUSH` only when Contents is now Read and write for the selected workbench repository; this declaration is persisted as operator-reported and not independently verified. Then hidden local credential input. No token in chat, arguments, raw output, receipts or persistent config.

The new consumed-attempt is flushed before one bounded receive discovery in a new EMPTY context. The inherited exact helper input remains `capabilities\nlist for-push\n\n`; no push/fetch/update/object transfer. Fixed endpoint only, no credential helper, redirects, TLS bypass, HTTP retry or arbitrary arguments. The two existing incident stages and all thirteen original records remain untouched and are revalidated. System Git, PATH and promisor stay unchanged.

New context (the utility creates it; do not pre-create or reuse):
`K:\Matawaka\Tools\WorkbenchV0601PermissionRecheckV1`

New evidence under `K:\Matawaka\Workbench\artifacts\publication-v0601`:
- `attempt-permission-recheck-v1-58b9430fc544998a8e40ba00b6757cc630ba9081.json`
- `permission-recheck-v1-58b9430fc544998a8e40ba00b6757cc630ba9081.json`

`PERMISSION_RECHECK_RECORDED_NO_PUSH` means the result is recorded, not access/publication success. Inspect `Probe.Category`, `AdvertisedBaselineMatches`, and `LocalSnapshotAndBothPriorStagesReverified`. Even successful discovery does not prove write/atomic/hook/branch-rule admission. No automatic next operation follows. Any future publication needs a separately qualified incident-linked successor and new explicit human confirmation; no renamed-attempt bypass.

On refusal/incomplete result preserve every old and new file, do not repeat any utility, do not delete stages/attempts, do not fabricate JSON. Return only the two new JSON or the safe refusal. No Update, Accept, standalone preflight replay, import, config repair, manual/API push or tooling merge to bound main.

## Qualification boundary

Only temporary fixtures, dummy credentials and loopback HTTPS in CI. The existing V2 verifier and discovery transport/classifier sources are pinned byte-for-byte. Reconciliation/orchestration tests add denied-incident, old-state, snapshot, new-confirmation, raw-byte, no-overwrite and exception checks. Old Windows qualification remains historical; new run must rerun the inherited suites. A shipped EXE requires completed Windows qualification, artifact/source/manifest binding and an independent downloaded-byte audit. Do not infer success merely from the existence of this source.
